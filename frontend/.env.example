# Backend API URL (full URL for development)
VITE_BACKEND_URL=http://localhost:8000

# API Base Path (use /api for relative or full URL for absolute)
# Default: /api (works with nginx proxy in production)
# Development: http://localhost:8000/api
VITE_API_URL=/api
