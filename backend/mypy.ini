[mypy]
plugins = sqlalchemy.ext.mypy.plugin
ignore_missing_imports = True
# Note: SQLAlchemy Column descriptors cause type checking issues with mypy
# This is a known limitation when not using SQLAlchemy 2.0+ with Mapped[] types
# For now, mypy runs informational-only in CI (with || true)
